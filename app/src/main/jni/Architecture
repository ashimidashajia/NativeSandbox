
    Geometry = raw list of vertices and faces 
    Shader = program and list of attributes 
    Texture = bitmap 
    Model = Geometry + Shader + Texture
    
    Instance = Model + List of attributes modifs
    
    
    Render loop = 
    
    for each Model : 
        set active shader
        set vtx array attributes 
        
        for each ModelInstance
            set instance attributes
            draw model
            
    
